name: attandace_teacher
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  bot_toast: ^4.1.3
  cupertino_icons: ^1.0.2
  flutter_network_connectivity: ^0.0.6
  flutter_svg: ^2.0.7
  get: 4.6.5
  http: ^1.1.0
  get_storage: ^2.1.1
  google_fonts: ^5.1.0
  intl: ^0.18.1
  sqflite: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo/
    - assets/illustrations/
    - assets/gif/
